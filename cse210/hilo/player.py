class Player:
    def __init__(self):
        self.score = 300

    def draw(self):
        """if ((self.guess == "l") and (self.nextCard <= self.currentCard)) or ((self.guess == "h") and (self.nextCard >= self.currentCard)):
            return True
        else:
            """
        return False
